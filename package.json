{
  "name": "wk-api-inmo",
  "version": "1.0.0",
  "description": "ApiInmoBase - Worker principal para procesamiento de URLs inmobiliarias con OpenAI",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "build": "tsc --noEmit",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "lint": "eslint src/**/*.ts",
    "test": "echo \"No tests configured yet\" && exit 0"
  },
  "keywords": [
    "cloudflare",
    "workers",
    "serverless",
    "edge",
    "openai",
    "real-estate"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240117.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.28.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
